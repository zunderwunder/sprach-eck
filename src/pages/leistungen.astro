---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/ui/Icon.astro';
import PrimaryButton from '../components/ui/PrimaryButton.astro';
import { Image } from 'astro:assets';
import sprachentwicklungBild from '../../public/images/Therapie-Sprachentwicklung-Aussprache.webp';
import myofunktionellBild from '../../public/images/Therapie-Myofunktionelle-Therapie.webp';
import kommunikationBild from '../../public/images/Therapie-Komplexe-Kommunikationsstörungen.webp';

// Hauptbereiche der Therapie
const mainAreas = [
  {
    title: 'Sprachentwicklung & Aussprache',
    description: 'Spezialisiert auf frühe Sprachentwicklung bei Kindern von 2-6 Jahren. Behandlung von Aussprache, Wortschatz und Grammatik mit interaktionellen Therapieansätzen.',
    icon: 'child',
    details: [
      'Phonetisch-phonologische Störungen',
      'Sprachentwicklungsverzögerungen',
      'Grammatik und Satzbau',
      'Wortschatzentwicklung'
    ],
    image: sprachentwicklungBild
  },
  {
    title: 'Myofunktionelle Therapie',
    description: 'Ganzheitliche Behandlung von Funktionsstörungen im Mund- und Gesichtsbereich. Begleitung bei kieferorthopädischen Behandlungen.',
    icon: 'pulse',
    details: [
      'Mundmotorik und Schlucken',
      'Sprechmuskulatur',
      'Zungenruhelage und Zungenband',
      'Kieferorthopädische Begleitung'
    ],
    image: myofunktionellBild
  },
  {
    title: 'Komplexe Kommunikationsstörungen',
    description: 'Erfahrung in der Behandlung komplexer Störungsbilder mit spezialisierten Therapiekonzepten.',
    icon: 'brain',
    details: [
      'Stottern (Non-Avoidance Ansatz)',
      'Selektiver Mutismus (MUTig Sprechen)',
      'Autismus Spektrum (KommAss)',
      'Auditive Verarbeitungsstörungen'
    ],
    image: kommunikationBild
  }
];

// Altersgruppen
const ageGroups = [
  {
    age: '2-6 Jahre',
    description: 'Kernzielgruppe: Frühe Sprachentwicklung',
    percentage: 60
  },
  {
    age: '7-10 Jahre',
    description: 'Schulkinder',
    percentage: 20
  },
  {
    age: '11+ Jahre',
    description: 'Jugendliche & Erwachsene',
    percentage: 20
  }
];

// Spezialisierte Methoden
const methods = [
  {
    name: 'Interaktionelle Therapie',
    description: 'Nach Nina Szammer für Sprachentwicklungsstörungen',
    icon: 'users'
  },
  {
    name: 'Neurofunktionstherapie (NF!T®)',
    description: 'Nach Elke Rogge',
    icon: 'brain'
  },
  {
    name: 'MUTig Sprechen',
    description: 'Nach Sabine Laerum für selektiven Mutismus',
    icon: 'heart'
  },
  {
    name: 'KommAss®',
    description: 'Nach Ulrike Funke für Autismus Spektrum Störungen',
    icon: 'star'
  }
];
---

<Layout 
  title="Therapie | Sprach Eck | Logopädie Baden"
  description="Professionelle Logopädie in Baden: Sprachentwicklung, Myofunktionelle Therapie und Kommunikationsstörungen. Individuelle Therapiekonzepte für Kinder und Jugendliche."
>
  <main class="py-24 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6">
      <!-- Hero Section -->
      <div class="text-center max-w-3xl mx-auto mb-32"
        x-data="{ show: false, hover: false }"
        x-intersect="show = true"
        x-bind:class="show ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-12'"
        class="transition-all duration-1000">
        <div class="relative inline-block mb-8"
          @mouseenter="hover = true"
          @mouseleave="hover = false">
          <!-- Verspielte Sprechblase -->
          <div class="absolute -top-16 left-1/2 transform -translate-x-1/2 w-48 p-3 bg-primary/10 rounded-2xl text-sm text-primary font-medium opacity-0 transition-all duration-300"
            x-bind:class="hover ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-4'">
            <div class="relative">
              Schön, dass du da bist! 💫
              <!-- Sprechblasen-Pfeil -->
              <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 translate-y-full">
                <div class="border-8 border-transparent border-t-primary/10"></div>
              </div>
            </div>
          </div>
          <h1 class="text-4xl md:text-5xl font-bold font-primary text-dark">
            Therapie & Leistungen
          </h1>
        </div>
        <p class="text-lg text-dark/70 font-secondary">
          Mit viel Erfahrung und modernen Therapiekonzepten begleite ich euch auf eurem individuellen Weg.
        </p>
      </div>

      <!-- Main Areas -->
      <div class="mb-32">
        <div class="space-y-32">
          {mainAreas.map((area, index) => (
            <div 
              class="relative"
              x-data="{ show: false }"
              x-intersect="show = true"
              x-bind:class="show ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-12'"
              style={`transition-delay: ${index * 200}ms`}>
              <div class={`grid md:grid-cols-2 gap-12 items-center ${index % 2 === 1 ? 'md:grid-flow-dense' : ''}`}>
                <!-- Image Section -->
                <div class={`relative rounded-2xl overflow-hidden aspect-[4/3] transition-all duration-300 group ${index % 2 === 1 ? 'md:col-start-2' : ''}`}>
                  <Image
                    src={area.image}
                    alt={area.title}
                    width={800}
                    height={600}
                    class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                    loading={index === 0 ? "eager" : "lazy"}
                    fetchpriority={index === 0 ? "high" : "auto"}
                  />
                </div>
                <!-- Content Section -->
                <div class="space-y-6">
                  <h3 class="text-2xl md:text-3xl font-bold font-primary text-dark">
                    {area.title}
                  </h3>
                  <p class="text-lg text-dark/70 leading-relaxed">
                    {area.description}
                  </p>
                  <ul class="space-y-3">
                    {area.details.map((detail) => (
                      <li class="flex items-center gap-3 text-dark/70">
                        <Icon name="check" class="w-5 h-5 text-primary flex-shrink-0" />
                        <span class="text-lg">{detail}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Age Groups & Focus Section -->
      <div class="mb-32 max-w-4xl mx-auto"
        x-data="{ show: false }"
        x-intersect="show = true">
        <h2 class="text-3xl md:text-4xl font-bold font-primary text-dark text-center mb-16">
          Mit wem ich zusammenarbeite
        </h2>
        
        <!-- Floating Circles -->
        <div class="grid md:grid-cols-3 gap-8 mb-20">
          {ageGroups.map((group, index) => (
            <div 
              class="relative group"
              x-bind:class="show ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8'"
              style={`transition: all 0.8s ease-out ${index * 200}ms`}>
              <div class={`
                aspect-square rounded-full p-8
                flex flex-col items-center justify-center text-center
                transition-all duration-500 transform group-hover:-translate-y-2
                ${index === 0 ? 'bg-primary/10 hover:bg-primary/20' : 'bg-accent/10 hover:bg-accent/20'}
                cursor-pointer
              `}>
                <span class="text-4xl font-bold font-primary mb-2">
                  {group.age}
                </span>
                <span class="text-lg text-dark/70">
                  {group.description}
                </span>
                {index === 0 && (
                  <span class="absolute -top-4 left-1/2 -translate-x-1/2 bg-primary/20 text-primary text-sm px-3 py-1 rounded-full">
                    Kernzielgruppe
                  </span>
                )}
              </div>
              
              <!-- Floating Decorative Elements -->
              <div class="absolute -z-10 inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-500">
                <div class={`absolute w-4 h-4 rounded-full ${index === 0 ? 'bg-primary/30' : 'bg-accent/30'} 
                  animate-float-slow top-0 left-1/4`}></div>
                <div class={`absolute w-3 h-3 rounded-full ${index === 0 ? 'bg-primary/20' : 'bg-accent/20'} 
                  animate-float-medium -top-6 left-1/2`}></div>
                <div class={`absolute w-2 h-2 rounded-full ${index === 0 ? 'bg-primary/10' : 'bg-accent/10'} 
                  animate-float-fast -top-4 right-1/4`}></div>
              </div>
            </div>
          ))}
        </div>
      </div>

<!-- Specialized Methods -->
<div class="mb-32 max-w-4xl mx-auto">
  <h2 class="text-3xl md:text-4xl font-bold font-primary text-dark text-center mb-4">
    Spezialisierte Therapiemethoden
  </h2>
  <p class="text-lg text-dark/70 text-center mb-12">
    Moderne und bewährte Therapieansätze für optimale Behandlungserfolge.
  </p>
  <div class="grid md:grid-cols-2 gap-8">
    {methods.map((method, index) => (
      <div
        class="group p-8 rounded-xl bg-light border border-primary/10 transition-all duration-300 hover:shadow-lg hover:shadow-primary/5 hover:-translate-y-1"
      >
        <div class="flex items-center gap-4 mb-4">
          <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center transition-transform duration-300 group-hover:scale-110 group-hover:rotate-3">
            <Icon name={method.icon} class="w-5 h-5 text-primary transition-transform duration-300 group-hover:-rotate-3" />
          </div>
          <div>
            <h3 class="text-xl font-bold font-primary text-dark group-hover:text-primary transition-colors duration-300">
              {method.name}
            </h3>
          </div>
        </div>
        <p class="text-dark/70 leading-relaxed">
          {method.description}
        </p>
      </div>
    ))}
  </div>
</div>
      <!-- CTA Section -->
      <div class="text-center">
        <PrimaryButton 
          href="mailto:kontakt@sprach-eck.at?subject=Anliegen%20und%20Kontaktaufnahmemöglichkeiten" 
          class="text-base px-6 py-3">
          Lassen Sie uns ins Gespräch kommen
        </PrimaryButton>
      </div>
    </div>
  </main>
</Layout> 

<style>
  .floating-shape {
    @apply absolute opacity-10 cursor-pointer pointer-events-auto;
    background: theme('colors.primary');
  }

  @keyframes shake {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    25% { transform: translate(-5px, 5px) rotate(-5deg); }
    75% { transform: translate(5px, -5px) rotate(5deg); }
  }

  .hover\:shake:hover {
    animation: shake 0.5s ease-in-out;
  }

  .shape-1 {
    width: 150px;
    height: 150px;
    border-radius: 63% 37% 54% 46% / 55% 48% 52% 45%;
    left: 5%;
    top: 15%;
    animation: float 14s ease-in-out infinite;
  }

  .shape-2 {
    width: 100px;
    height: 100px;
    border-radius: 38% 62% 63% 37% / 41% 44% 56% 59%;
    right: 10%;
    top: 20%;
    animation: float 12s ease-in-out infinite reverse;
  }

  .shape-3 {
    width: 75px;
    height: 75px;
    border-radius: 49% 51% 48% 52% / 57% 44% 56% 43%;
    left: 15%;
    bottom: 20%;
    animation: float 16s ease-in-out infinite;
  }

  .shape-4 {
    width: 125px;
    height: 125px;
    border-radius: 53% 47% 59% 41% / 47% 53% 47% 53%;
    right: 15%;
    bottom: 15%;
    animation: float 18s ease-in-out infinite reverse;
  }

  @keyframes float {
    0% { transform: translate(0, 0) rotate(0deg); }
    33% { transform: translate(30px, -30px) rotate(120deg); }
    66% { transform: translate(-20px, 20px) rotate(240deg); }
    100% { transform: translate(0, 0) rotate(360deg); }
  }

  @keyframes float-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @keyframes float-medium {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-15px); }
  }

  @keyframes float-fast {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
  }

  .animate-float-slow {
    animation: float-slow 4s ease-in-out infinite;
  }

  .animate-float-medium {
    animation: float-medium 3s ease-in-out infinite;
  }

  .animate-float-fast {
    animation: float-fast 2s ease-in-out infinite;
  }
</style> 